DROP TABLE IF EXISTS TURMA;

CREATE TABLE TURMA (
id INT AUTO_INCREMENT PRIMARY KEY,
nome VARCHAR(30) NOT NULL,
total INTEGER NOT NULL
);

insert into turma values (1, 'TURMA 1', 10);

insert into turma values (2, 'TURMA 2', 20);

